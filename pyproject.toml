[project]
name = "helper-scripts-vllm"
version = "0.1.0"
description = "Scripts for markup HELPER"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "aiohttp>=3.14.0",
    "requests",
    "tqdm",
    "transformers",
    "vllm==0.14.0",
    "xformers",
]